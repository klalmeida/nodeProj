/**
 * @file Declares MaritalStatus enum giving the options for
 * the type of relationship status of a user
 */
enum MaritalStatus {
    Married = "MARRIED",
    Single = "SINGLE",
    Widowed = "WIDOWED"
};

export default MaritalStatus;