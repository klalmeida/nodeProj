/**
 * @file Declares AccountType enum giving the options for the
 * type of user account that can be created
 */

enum AccountType {
    Personal = "PERSONAL",
    Academic = "ACADEMIC",
    Professional = "PROFESSIONAL"
};

export default AccountType;